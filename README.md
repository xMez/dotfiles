# dotfiles

### Install deps
```console
sudo apt update
sudo apt install fish make
chsh -s /usr/bin/fish
```

### Setup everything
```console
export GIT_USER=<user>
export GIT_MAIL=<email>
make
```

### Install docker
```
make docker
```

